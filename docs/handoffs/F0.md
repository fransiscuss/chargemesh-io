# F0 handoff — foundation

Status: complete. Implementation commit: `e6a1c3d` on `main`, pushed to origin.
[CI passed](https://github.com/fransiscuss/chargemesh-io/actions/runs/35301531300).

Delivered pnpm/Turbo workspace shells, shared strict TypeScript/ESLint/Prettier
configuration, Vitest coverage gates, reusable CI, Compose QA services, all 12
Drizzle tables, generated SQL migrations, a PGlite helper, and AES-256-GCM crypto.
Primary code: `packages/db/src`, `packages/db/migrations`, `packages/config`.

Verification: install, lint, typecheck, build, and 29 tests passed; 96% line
coverage. Applied `pnpm db:migrate` twice to the user-supplied Neon development
database. CI also applied the migration runner twice against Postgres 16.
Reviewed schema constraints, generated defaults, credential handling, and cleanup.

Decisions: nullable parsed message fields preserve malformed frames; connection
ULIDs have no foreign key so retention can remove connections independently;
unknown-charger alerts permit a null charger ID; encrypted JSON configuration is
stored as a ciphertext string; bigint IDs never pass through JS numbers.

Environment: Node 24, pnpm 10.14.0. Root `.env` is ignored and contains the supplied
database connection string. Do not expose it. Docker was unavailable at this
stage; F2 subsequently tested the Compose services.

Resume: F1 was next and has since completed; see `F1.md`. Startup environment
parsing is added as each application becomes executable. No F0 blocker remains.
