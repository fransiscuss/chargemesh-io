# F1 handoff — OCPP library

Status: complete. Implementation commit: `409ca4d` on `main`, pushed to origin.
[CI passed](https://github.com/fransiscuss/chargemesh-io/actions/runs/35302178269).

Delivered `packages/ocpp/src`: frame codec with original raw strings, subprotocol
negotiation, Ajv validation, call correlation, version-specific errors,
transaction/status helpers, and the empty adapter registry. Fixtures live in
`packages/ocpp/test/fixtures`; unchanged schema collections in `packages/ocpp/schemas`.

Verification: lint, typecheck, build, and 90 repository tests passed. OCPP line
coverage: 100%. Every vendored schema compiled. Required request/response fixtures
validated. Built ESM imports worked. Vendored files matched their source exactly.
Reviewed rewriting without mutation, correlation direction/expiry, raw forwarding,
schema packaging, and attribution.

Decisions: `serializeFrame` uses structured fields; forwarding uses `frame.raw`.
Validation uses `uri-js` because the pinned `ocpp-rpc@2.2.1` schemas have legacy
URN IDs. The package exports `dist/src/index.js` and ships JSON under `dist/schemas`.
The schema notice distinguishes OCA CC BY-ND 4.0 from the library's MIT license.
`callError` defaults to 1.6 and takes an optional version as its fourth argument.

Resume: F2 was next and has since completed; see `F2.md`. F4 must resolve the
external representation of malformed frames, as recorded in spec Open questions.
There is no protocol translation implementation or registry entry.
