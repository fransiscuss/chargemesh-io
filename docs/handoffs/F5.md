# F5 handoff — deploy & CI/CD (push to Fly)

Status: implementation complete, CI green; first production deploy outstanding
(user Fly setup required — see below). Implementation commit: `917b325`,
pushed to `main`. [CI passed](https://github.com/fransiscuss/chargemesh-io/actions/runs/35308870429)
(includes the new hadolint + actionlint steps).
This handoff is published in the documentation commit immediately after implementation.

## Delivered

- `Dockerfile` (repo root): multi-stage — `turbo prune gateway --docker` →
  frozen-lockfile install → build → `node:24-slim` runner as non-root user
  `chargemesh` (uid/gid 1001). One image serves both processes; default CMD is
  the gateway, worker via Fly process override. The separate
  `packages/sim/Dockerfile` (mock-csms) is unaffected.
- Gateway entry restructure: bootstrap moved verbatim from `src/index.ts` to
  new `src/main.ts` (`apps/gateway/dist/main.js`); `src/index.ts` is library
  exports only; new `src/worker.ts` (`dist/worker.js`) is an F14 placeholder
  that stays alive, handles SIGTERM/SIGINT and does no sink work. Package
  `start` script now runs `dist/main.js`; `scripts/steve-smoke.mjs` still
  imports the intact `dist/index.js` library entry.
- `fly.toml`: `[processes]` gateway/worker per spec, `[http_service]` on 8080
  with `auto_stop_machines="off"`, `min_machines_running=1` and a `/healthz`
  check, `[[services]]` on 50051 with `handlers=["tls"]` +
  `tls_options.alpn=["h2"]`, `[deploy] release_command` running
  `node packages/db/dist/migrate.js` (migrations run only here).
- `.github/workflows/deploy-gateway.yml`: spec shape — push-to-main path
  filter + workflow_dispatch, `deploy-gateway` concurrency (no cancel),
  `test` job reusing `./.github/workflows/ci.yml`, `deploy` job (needs test,
  `production` environment) with flyctl setup pinned to real commit
  `fc53c09e1bc3be6f54706524e3b82c4f462f77be` (verified via GitHub API).
- `docs/RUNBOOK.md`: Fly app setup, certs for `ocpp.`/`api.`, Cloudflare
  DNS-only records, secrets (names only), post-deploy sim smoke, rollback.
  No secrets in the file.
- `ci.yml`: added hadolint (`hadolint/hadolint-action@v3.5.0`, tag verified)
  and actionlint (pinned `v1.7.12` release tarball, release verified) steps.
- `shutdown.test.ts`: the existing F3 test already satisfies the F5 unit-test
  requirement (fakes assert stop → close(1001) → flush → exit order); no
  extension needed. New `worker.test.ts` covers placeholder start/stop
  ordering and idempotent stop.
- `vitest.config.ts`: coverage excludes `apps/gateway/src/main.ts` (entry
  bootstrap, previously excluded when it lived in `index.ts`); `.dockerignore`
  extended with `**/coverage` and `**/.next`.

## Verification and review

`pnpm lint` (forced fresh run, not turbo-cached), `pnpm typecheck`,
`pnpm build`, `pnpm test` all pass: 183 tests (29 files; 182 preserved + 1 new
worker test). Coverage gates hold: recorder 97.08%, session 98.32%, pipeline
100%, worker 89.47% (only the `import.meta.main` entry lines uncovered).

Independent orchestrator verification on top of the delegate's:
- All three third-party pins confirmed real via the GitHub API (flyctl SHA,
  hadolint `v3.5.0` tag, actionlint `v1.7.12` release + asset URL).
- `fly.toml` parses (TOML) with the spec's processes/ports; all three
  workflows parse (YAML); actionlint `v1.7.12` binary run locally reports no
  issues.
- `docker build -f Dockerfile .` succeeds (delegate) and the produced image
  was re-verified by the orchestrator: boots as `uid=1001(chargemesh)`,
  `/healthz` → `{"ok":true,"chargersConnected":0}`; the worker entry starts,
  handles SIGTERM and exits cleanly. (First boot attempt correctly failed fast
  on env validation with dummy secrets — fail-fast behaviour intact.)
- Review covered: turbo-prune docker staging, non-root runner file ownership,
  release-command path matching the real db build output, reusable-workflow
  `test` job wiring, deploy concurrency, RUNBOOK accuracy (corrected a false
  "(already done)" claim about app creation), and F0–F4 preservation.

CI on the push ([run](https://github.com/fransiscuss/chargemesh-io/actions/runs/35308870429)):
success, including the hadolint and actionlint steps.

## Outstanding — genuine blocker (user Fly setup)

The push triggered `deploy-gateway`
([run](https://github.com/fransiscuss/chargemesh-io/actions/runs/35308870572)):
`test / checks` succeeded; `deploy` failed with `FLY_API_TOKEN: (empty)` →
`Error: no access token available`. No Fly app, token, secrets, DNS or certs
exist yet, and creating them requires the user's Fly account — not attempted.

To finish F5's Done-when, the user must follow `docs/RUNBOOK.md` §1+§4:
`fly apps create chargemesh-gw`, allocate IPs, `fly secrets set …`,
`fly certs add ocpp./api.`, Cloudflare DNS-only records, and
`FLY_API_TOKEN` (`fly tokens create deploy -a chargemesh-gw`) in GitHub
Actions secrets; then re-run the workflow (`workflow_dispatch`) or push.
After that: `pnpm sim --url wss://ocpp.chargemesh.io/ocpp/SMOKE01 …` should
complete a full session, and a redeploy should cause only a reconnect.

## Environment and resume

Node 24, pnpm 10.14.0. Root `.env` is ignored; never print it. Local Docker
verification used the `desktop-linux` context (`docker.exe`). Evidence image
`chargemesh-gw:f5local` remains in that context; test containers were removed.
SteVe/MariaDB compose services were up but uninvolved in F5.

Next: F6 only — but note F5's production-deploy gate above is still open.
Per AGENTS.md the primary agent orchestrates and delegates the next eligible
feature to a sub-agent (Muse Spark — do not use `gpt-5.6-luna`; its billing is
insufficient). Read AGENTS.md, shared spec sections 1–3 and F6; inspect the
current interfaces (pipeline sync/async ordering, `FrameContext.delivery`,
recorder validator ordering for mirror-response recording). Preserve all F5
tests. Do not start F7 until F6 meets all feature gates and has its own handoff.
